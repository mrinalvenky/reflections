Need to do git init to initialize a repository. After the init there is nothing commited in the repository and all files are marked as untracked. Used git status to see the current status of the files which shows all are untracked. git log gives an error.
